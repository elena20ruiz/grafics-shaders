# Resum Modificar Objectes:

## Funcions interesants:

- *distance(V,F)*: Distancia entre dos vectores
- *clamp(x,y,x)*: Interpolació lineal
- *pow(x,n)*: x^n
- *normalize(vecx)*: Normalizar vector

	
